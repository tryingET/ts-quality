---
summary: "GitHub release notes for ts-quality v0.3.1."
read_when:
  - "When creating the public GitHub Release for ts-quality v0.3.1"
type: "draft"
---

# GitHub release draft — ts-quality v0.3.1

## Title

`ts-quality v0.3.1 — public verifier parser fix`

## Release body

This release is prepared through the repo's GitHub Release authority path. Publishing the GitHub Release for tag `v0.3.1` triggers `.github/workflows/publish.yml` in the `npm-publish` environment, which validates the tag/version contract, re-runs deterministic package proof, publishes the staged package to npm through Trusted Publishing/OIDC, verifies public installability, and attaches the proven tarball to the GitHub Release.

### Breaking Changes
- None.

### Added
- None.

### Changed
- None.

### Fixed
- Fixed the public release verifier's compact doctor parser so prose fields containing `=` (for example `NODE_OPTIONS=--enable-source-maps`) do not fail an otherwise valid `doctor --machine` protocol packet.

### Agent migration notes
- No breaking-change migration is required for agents beyond ordinary release-note review.

### Verification before release

```bash
npm run verify --silent
RELEASE_TAG=v0.3.1 GITHUB_REF_TYPE=tag npm run release:intent:check --silent
```

### Install

```bash
npx -p ts-quality@0.3.1 ts-quality --help
```
